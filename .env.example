# MeatyCapture Environment Configuration
# Copy this file to .env and customize for your environment
# See src/server/index.ts for implementation details

# Web App Port Configuration
# Port number for the web frontend (nginx)
# Default: 80 (use 3000 if port 80 is restricted)
WEB_PORT=80

# HTTP Server Configuration
# Port number for the REST API server
# Default: 3737 (3001 for Docker)
PORT=3737

# Server Environment Mode
# Controls logging verbosity and development features
# Values: development | production
# Default: development
NODE_ENV=development

# Data Directory
# Base directory for storing request-log markdown files and project metadata
# Supports tilde (~) expansion for home directory
# Default: ~/.meatycapture
MEATYCAPTURE_DATA_DIR=~/.meatycapture

# Logging Level
# Controls verbosity of structured logs from server
# Values: debug | info | warn | error
# Default: debug (development), info (production)
LOG_LEVEL=debug

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# Use '*' for development (allows all origins)
# In production, specify exact origins: https://app.example.com,https://www.example.com
# Default: * (development only)
CORS_ORIGINS=http://localhost:5173,http://localhost:4173,http://localhost:3000

# Authentication Token (Optional)
# Bearer token required for API authentication
# If not set, all requests are allowed (development mode)
# Format: Any non-empty string - used as Bearer token in Authorization header
# To authenticate requests, include header: Authorization: Bearer {MEATYCAPTURE_AUTH_TOKEN}
# MEATYCAPTURE_AUTH_TOKEN=your-secret-token-here

# API URL for Client Configuration
# Base URL for clients (web app, mobile, etc.) to connect to this server
# Used in client configuration for API endpoint discovery
# Examples: http://localhost:3737, https://api.example.com
# Default: http://localhost:3737
# Note: For Docker builds, this is set via docker-compose.yml build args
# MEATYCAPTURE_API_URL=http://localhost:3737
