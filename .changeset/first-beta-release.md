---
"meatycapture": minor
---

First beta release of MeatyCapture CLI for npm distribution.

This release includes:
- CLI commands for creating and managing request log documents
- Project management and configuration
- Local file storage mode (default) and API mode support
- Global installation via `npm install -g meatycapture`
- Binary aliases: `meatycapture` and `mc`
